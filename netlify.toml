[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20"

# SPA routing (all paths -> index.html)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
